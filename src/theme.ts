import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: `'DaysSansBlack', sans-serif`,
    body: `'DaysSansBlack', sans-serif`,
  },
});

export default theme;